#include <stdio.h>
#include <stdlib.h>

int main()
{
    int *a;
    char *b;
    float *c;
    printf("the value of integer pointer is%d\n",sizeof(a));
     printf("the value of integer pointer is%d\n",sizeof(b));
      printf("the value of integer pointer is%d\n",sizeof(c));
}
