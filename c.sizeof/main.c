//write a program to show size of operator
#include<stdio.h>
int main()
{
    float b;
    int a;
    char c;
    double d;
    printf("value of a which is integer type=%d \n",sizeof(a));
    printf("value of a which is float type=%d \n",sizeof(b));
     printf("value of a which is char type=%d \n",sizeof(c));
      printf("value of a which is double type=%d \n",sizeof(d));
    return 0;
}
